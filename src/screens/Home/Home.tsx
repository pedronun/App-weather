import { SafeAreaView, Text } from 'react-native';

const Home = () => {
  return (
    <SafeAreaView style={{backgroundColor: '#000', flex: 1}}>
      <Text>Home</Text>
    </SafeAreaView>
  );
};

export default Home;